package com.company;

public class OrderedList {
    // Will contain two overloaded implementations of a method named checkSorted, which determines whether a List
    // object, supplied as a parameter, is in strictly ascending order.
}
